export const findAllUsers = () => {
  return new Promise((resolve, reject) => {
    // if (errorCondition) {
    //   reject(new Error('Error message'));
    // } else {
      resolve(users);
    // }
  })
}